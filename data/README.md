# Data

Place your CSV datasets here. The default sample is `titanic.csv`.

Override paths in `.env` or when running:

```bash
$env:DATASET_PATH = "data/your_dataset.csv"
$env:TARGET_COLUMN = "your_target_column"
crewai run
```
